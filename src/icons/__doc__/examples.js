import { RamblerAutoIcon } from 'rambler-ui/icons/services'
import RamblerMailIcon from 'rambler-ui/icons/services/RamblerMailIcon'

export default function () {
  return (
    <div>
      <RamblerMailIcon color='dark' size={30} style={{margin: 10}} />
      <RamblerAutoIcon size={30} style={{margin: 10}} />
    </div>
  )
}
